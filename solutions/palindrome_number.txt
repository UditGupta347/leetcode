/*
 * Approach/Algorithm:
 * This solution reverses the given integer and compares it with the original.
 * It avoids converting the integer to a string by using mathematical operations.
 *
 * Time Complexity: O(log(n)), where n is the value of the input integer.
 * Space Complexity: O(1)
 */

            reverse = (reverse * 10) + (x % 10);
            x /= 10;
        }

        return reverse == xcopy;
    }
}